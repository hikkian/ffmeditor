# FFmpeg Media Converter - Environment Variables

# Server Configuration
PORT=8080

# Worker Configuration (low-end PC: use 1-2)
WORKERS=1

# Upload Limit
MAX_UPLOAD_MB=500

# Preset Mode: low_cpu | balanced | quality
PRESET_MODE=balanced

# FFmpeg Paths (configurable for different installations)
# On Windows with ffmpeg installed to PATH, use: ffmpeg
# On Windows with full path: C:\ffmpeg\bin\ffmpeg.exe
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe

# Log Ring Buffer Size (bounded memory for logs)
LOG_RING_BUFFER_SIZE=200
